// This file can he replaced during build by using the 'ftIeRepIacements' array.
// 'ng bui[W replaces •environment. ts- with •environment. prod. ts
// The List of file replacements can be found in 'angular.json

export const environment = {
  production: false,
  api_key: '397c42a96d2c187c2a4912fccc6be558',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to import zone related error stack frames such as `zone.run`, `zoneDelegate.InvokeTask`.
 *
 * This import should be comented out in production mode, because it will have negative impacts
 * on performance if an error is thrown.
 *
 * import zone.js/plugins/zone-error'; // Included with angular cli
 *
 */
